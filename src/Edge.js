// ---------------------------------------------------------------------------
// Edge methods
//

const Edge = function(lSite, rSite) {
  this.lSite = lSite;
  this.rSite = rSite;
  this.va = this.vb = null;
};

export default Edge;
